<div class="text-center">
<%= @dog.name %>
<% if @dog.photo? %>
  <%= cl_image_tag @dog.photo.path, width: 200, height: 300, crop: :fill %>
<% end %>
<%= link_to "Book a play date with me!", new_dog_booking_path(@dog) %>
</div>
